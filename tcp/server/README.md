Examples of simple TCP servers

* sigio-server.c: a simple TCP multi-client server with signal based notification
* epoll-server.c: epoll-based server for concurrent clients - also gets signals through epoll
